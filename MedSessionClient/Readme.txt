1. BTFClient ID must be unique in a run.

2. BTFClient cannot be passed between objects.

3. scripts/ folder contains the scripts for adding records into the blockchain service and the directory service for warm up.